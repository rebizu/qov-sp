{
  "name": "qov-recorder-player",
  "version": "1.0.0",
  "description": "QOV (Quite OK Video) recorder and player",
  "type": "module",
  "scripts": {
    "dev": "npx vite",
    "build": "npx tsc && npx vite build",
    "preview": "npx vite preview"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.10"
  },
  "dependencies": {
    "jsmpeg-player": "^3.0.3"
  }
}
